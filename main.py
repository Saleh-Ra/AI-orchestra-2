"""Legacy entry point — use `src/main.py` after Phase 1 scaffold."""

if __name__ == "__main__":
    print("AI Orchestra 2 is not installed yet.")
    print("After Phase 1, run: uv run python src/main.py")
